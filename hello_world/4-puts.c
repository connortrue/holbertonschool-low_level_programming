#include <stdio.h>
/**
 * main - This prints text to the screen.
 *
 * Return: Returns 0 when it works.
 */

int main(void)
{
	puts("Programming is like building a multilingual puzzle");
	return (0);
}
